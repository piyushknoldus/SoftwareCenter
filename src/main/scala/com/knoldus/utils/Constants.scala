package com.knoldus.utils

object Constants {

  val started = "Started building your docker image"
  val completed = "...... Docker Image Created Successfully ...... Type in another shell command :" +
    " 'docker images' to view AND to run :'docker run -it imageID /bin/bash'"
}
